rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // EduVibe School Cloud - Allow full real-time collaboration for classrooms, teachers, and students
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
